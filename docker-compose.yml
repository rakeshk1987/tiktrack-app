version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules # Anonymous volume to bypass WDPassport noexec restriction!
    environment:
      - CHOKIDAR_USEPOLLING=true # Ensures Vite Hot Reload works across USB drive mounts
      - WDS_SOCKET_PORT=0
